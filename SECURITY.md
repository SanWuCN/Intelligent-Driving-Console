# 安全说明

## 实车安全

本项目可以向实车底盘发送速度和转向指令。所有调试必须：

- 使用封闭、可控的测试场地。
- 首次验证优先架空驱动轮。
- 保持物理急停和遥控接管随时可用。
- 启动循迹前复核定位、路径方向、速度、转向极性和障碍距离。
- 不得将实训室参数直接用于公开道路或高速运行。

网页急停依赖 Jetson、网络、Docker、ROS 和 CAN 链路正常，不能替代硬件急停。

## 访问控制

- 实训环境默认控制令牌为 `801801801`，只允许在受信任局域网内使用；跨网络部署前必须替换。
- 不要将 Jetson 密码、SSH 私钥或 GitHub Token 写入仓库。
- 控制台默认监听 `0.0.0.0:8765`，应只在受信任局域网中使用。
- 如需跨网络访问，应增加 TLS、防火墙、VPN 和更强的身份认证。
- 怀疑令牌泄露时，立即生成新的随机令牌。

## 仓库安全边界

- `runtime/config.json`、日志和 PID 文件已被 `.gitignore` 排除。
- 后端只接受固定动作名称，不提供任意 Shell 命令接口。
- PCD/CSV 文件必须来自配置的数据目录，并进行扩展名和路径穿越检查。
- 除急停外，写操作都需要 `X-Control-Token`。

## 报告问题

请不要在公开 Issue 中张贴密码、令牌、SSH 信息、车辆实时位置或未脱敏日志。
